{
  "name": "clinica-medica-site",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "frontend": "npm run dev --workspace=frontend",
    "backend": "npm run dev --workspace=backend",
    "install:all": "npm install --workspaces"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
